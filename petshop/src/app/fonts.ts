import { Ubuntu, Amatic_SC } from "next/font/google";

export const inter = Ubuntu({
  weight: ["400", "500", "700"],
  subsets: ["latin"],
});

export const amantic_sc = Amatic_SC({
  subsets: ["latin"],
  weight: "700",
  variable: "--font-amantic",
});
